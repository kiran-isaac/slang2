String A = readLine()

focus String guess

Limited A of only (Int : remaining, [A] : stack) can {
  new(Int n) {n; []}
  
  @override Box bool push(A a) {
    if (remaining == 0) {
      return Box bool false
    }

    stack.push(a)
    
    return Box bool true    
  }
}

while (pop() != A.peek()) {

}